# Chat Psychology Analyzer Backend

Бэкенд для анализа переписки с помощью ИИ на базе FastAPI.

## Возможности

- 🧠 **Анализ переписки** — полный психологический разбор с помощью ИИ
- 📊 **Мета-информация** — быстрая статистика по переписке
- 🔐 **Конфиденциальность** — анонимизация данных
- 💬 **Поддержка форматов** — обработка TXT и HTML файлов чатов
- 🗂️ **Поддержка ZIP-архивов** — обработка архивов с TXT-файлами чата
- 📅 **Фильтрация по датам** — анализ только выбранного периода

## API Endpoints

### `POST /chat_meta`

Быстрая статистика по переписке без использования ИИ.

**Тело запроса:**
```json
{
  "chat_text": "string"
}
```

**Ответ:**
```json
{
  "stats": {
    "total_messages": 0,
    "participants": [],
    "first_message_at": "string",
    "last_message_at": "string"
  },
  "snippet_bytes": 0,
  "upload_bytes": 0,
  "recommended_bytes": 0
}
```

### `POST /analyze_chat`

Полный анализ переписки с ИИ.

**Тело запроса:**
```json
{
  "chat_text": "string",
  "range_from": "YYYY-MM-DD",
  "range_to": "YYYY-MM-DD"
}
```

**Ответ:**
```json
{
  "participants": [],
  "relationship": {},
  "recommendations": [],
  "stats": {}
}
```

### `POST /analyze_zip`

Обработка ZIP-архива с TXT-файлами чата.

**Формат запроса:**
Multipart/form-data с файлом ZIP

**Ответ:**
```json
{
  "chat_text": "string"
}
```

## Установка

```bash
pip install -r requirements.txt
```

## Запуск

```bash
uvicorn app.main:app --reload
```

## Лицензия

MIT